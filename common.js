
const PROTOCOL_CONFIG = {
    PAYLOAD_SIZE: 1800,
            TRANSMISSION_INTERVAL: 500, // 送信側がQRコードを切り替える時間間隔(ミリ秒)
};

const QR_CONFIG = {
    ERROR_CORRECTION_LEVEL: 'M',
    TYPE_NUMBER: 40,
};
